# Empty test application

A minimal application linked to the framework.

## Build and run

Follow the [framework configuration instructions](../../README.md#configuring), select `empty`, and build for Windows and Android. Follow the [run instructions](../../README.md#running).

When creating a new target from this test, update its CMake project name and add its selection to `Config.txt`. Run Windows executables with the test directory as the working directory.
